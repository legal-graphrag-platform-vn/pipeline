# Core
pydantic>=2.7
pydantic-settings>=2.3
typer>=0.12

# Crawler
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
playwright>=1.45

# Parser
pymupdf>=1.24
pypdf>=6.0             # backend trích text thay thế cho PDF có text layer thật (xem --backend pypdf)
pytesseract>=0.3.13   # OCR binding — yêu cầu cài Tesseract OCR engine riêng (binary), xem README
Pillow>=10.0           # chuyển PyMuPDF pixmap -> ảnh cho pytesseract

# LLM extraction
google-genai>=0.3
openai>=2.0.0
tenacity>=8.3

# Dev / test
pytest>=8.2
python-dotenv>=1.0
